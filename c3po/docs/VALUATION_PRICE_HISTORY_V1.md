# Valuation V3.2 — série de preços persistida (`valuation_price_history/<M>`), implementação rev 3

Referência normativa: spec V3.2 rev 7 (congelada a três, `d31ed90e…`), §2.2 "Série de rótulos" e §9.2; decisão do dono de 07/09/2026 (36 meses de histórico como treino; recibo `093aae44…`). Rev 2 respondeu aos achados C394-1..5 do Codex (recibo `9577cfd2…`) e C394-6..11 da pré-revisão adversarial interna. **Rev 3 responde aos cinco residuais do Codex sobre a rev 2:** C394-2 residual (captura ≠ publicação), C394-9 residual (seção crítica por mercado), C394-12 (três relógios por barra), C394-13 (hash recomputado na leitura; cópias), C394-14 (sessão/campo das linhas rejeitadas; `adjustment_unknown`). **Residuais da verificação adversarial da rev 3 (R1–R8), todos fechados nesta revisão:** R1 sessão recusada que já tinha barra em safra anterior não é "desaparecida" (`adjustment_unknown`, nunca `vintage_unreproducible`); R2 leitura de dedupe dentro da transação da captura, depois da trava; R3 resultado do produtor é cópia profunda; R4 relógios não parseáveis = `manifest_mismatch`, nunca exceção; R5 só `ok` entra no cache; R6 `backfill`/`nightly` leem o relógio exatamente três vezes; R7 DDL/docstrings alinhados; R8 safra vazia recusada antes de escrever. **Segunda reverificação (R9–R14), também fechada:** R9 `run_all` dá a vez a todo mercado (um mercado recusado não aborta os outros; a fase falha no fim nomeando só os recusados); R10 publicação anterior sem `fetched_at` parseável é recusada explicitamente, por id, e o escritor de publicação recusa gravar uma linha que não ateste a captura conferida; R11 `available_at` atestado tem de ser o próprio `published_at` da publicação; R12 duplo em memória guarda/devolve cópias profundas e a captura é atômica; R13 mensagem da R8 conta erros, símbolos com todas as linhas recusadas e respostas vazias em separado; R14 conjunto pedido vazio (sem cobertura) é recusado antes de ler o relógio. **Terceira reverificação (S1–S7), fechada:** S1 `run_all` corre cada mercado **no máximo uma vez por fase** (um mercado cuja última publicação é igual ou posterior ao instante em que a fase venceu é saltado sem requisição nem escrita — uma nova tentativa do worker refaz só o mercado que falhou); S2 `backfill`/`nightly` recusam conjunto vazio **antes do primeiro tick do relógio** (helper único `_wanted`); S3 o rollback em memória remove a própria linha do manifesto (`list.remove`), nunca reconstrói a lista; S4 `latest_analysis_snapshot`/`latest_analysis_snapshot_before` em memória devolvem cópias profundas; S5 o duplo em memória espelha os `CHECK`s da DDL (mercado no enum, `close`/`adjusted_close` > 0, `bar_sha256` 64 hex minúsculos); S6 o escritor de publicação exige `inputs.fetched_at` como **string** ISO-8601 antes da trava; S7 runbook da publicação envenenada e escopo do pyright (abaixo). **Quarta reverificação (T1–T5), fechada:** T1 runbook R10 reescrito com verdade (enquanto a linha envenenada for a última publicação, os dois escritores recusam por id qualquer que seja o schema — subir `SCHEMA_VERSION` não ajuda; só há saídas manuais sob ordem da mesa: corrigir o `inputs.fetched_at` da linha ou inserir uma publicação bem formada do mesmo manifesto depois dela); T2 `run_all`/`offhours_due_at` leem `now`/`due_at` **ingénuos como hora de São Paulo**, exatamente como `_phase_is_due`/`start_of_today` do worker; T3 `_positive_price` compara no próprio tipo (um int gigante é um `NUMERIC`, nunca `OverflowError`); T4 `session_date` tem de ser data ISO canónica e `volume`, quando presente, número finito; o duplo em memória guarda o **espelho JSON** (`json.loads(json.dumps(…, default=str))`) de `inputs`/`outputs` de toda snapshot, como a coluna `jsonb`; T5 o salto "já publicou desde que a fase venceu" ganhou a forma forte: o `due` viaja `run_all → nightly → persist_run → escritor da captura` e é **reconferido dentro da trava do mercado**, imediatamente antes da escrita (`AlreadyPublishedError`, um salto, nunca falha).

## O que é

- **Uma safra por corrida, em dois fatos.** Cada corrida (backfill ou noturna) busca a **janela inteira** (36 meses por padrão, `C3PO_VALUATION_PRICE_HISTORY_BACKFILL_MONTHS`) para todo o conjunto de cobertura e grava:
  1. a **captura**: um manifesto em `analysis_snapshots` (`analysis_type = valuation_price_history`, `entity_key = <M>`, `published_at` = `fetched_at` = instante lido **depois da última resposta** do provedor) **e** as barras cujo conteúdo mudou, **na mesma transação** (as barras referenciam o manifesto por FK; lotes de 5.000 via `executemany`);
  2. a **publicação**, gravada **só depois do commit da captura**: uma linha append-only `analysis_type = valuation_price_history_publication`, `entity_key = <M>`, `published_at` = **`available_at`** = relógio lido **imediatamente antes desse insert** (o último passo antes da escrita), `inputs = {schema_version, price_snapshot_id, fetched_at, started_at, available_at}`, `outputs = {schema_version, bars_sha256, price_snapshot_id}`.

  O manifesto grava `started_at`, `fetched_at`, janela, calendário (nome + versão do `exchange_calendars`), símbolos pedidos/com barras/ausentes, erros do provedor por símbolo, **linhas rejeitadas por símbolo com sessão e campos ausentes** (`rows_rejected = {símbolo: [{session, missing: [...]}]}`, mais as contagens `rows_rejected_incomplete`/`rows_rejected_total`; uma linha rejeitada fora da janela não é evidência e não entra), sessões duplicadas na mesma resposta (`rows_duplicate_in_response`; a primeira linha vence), **um hash de série por símbolo** (`series_sha256`), contagem de barras por símbolo, símbolos `unreproducible` e `bars_sha256`.
- **Um manifesto sem publicação é "capturado, nunca publicado"**: nenhum leitor o vê, nunca; `last_run_at` do worker lê as publicações, não os manifestos.
- **Tabela append-only `valuation_price_bars`** (migração `db/049_valuation_price_history.sql`): uma linha por `(mercado, símbolo, sessão, fonte, fetched_at)` com `close` **e** `adjusted_close` (ambos obrigatórios; preços e volume canonizados a 6 casas para que o valor lido do `NUMERIC` seja o mesmo float que foi hasheado), `volume`, `currency`, `provider_symbol`, `snapshot_id` (manifesto da corrida) e `bar_sha256` = **hash do conteúdo** (sem o relógio). Uma corrida insere **só as barras cujo conteúdo mudou** em relação à última linha da mesma `(mercado, símbolo, sessão, fonte)`; barra igual não custa armazenamento. Nada é atualizado, apagado ou truncado (gatilhos).
- **Seção crítica por mercado (C394-9, R2).** Da captura à publicação a corrida segura a trava do mercado: em memória um `RLock` por mercado (`Database.price_history_lock`); em PostgreSQL cada escritor (captura e publicação) abre a transação com `SELECT pg_advisory_xact_lock(hashtext('valuation_price_history:' || <M>))`, **relê** o manifesto e a publicação anteriores e **recusa** (sem escrever nada) uma captura que não avance além do `fetched_at` do manifesto anterior **e** do `available_at` da publicação anterior. **A leitura de dedupe acontece dentro dessa mesma transação, depois da trava e das rechecagens de relógio**: `Database.persist_price_history_run` lê os últimos `bar_sha256` por `(símbolo, sessão)` (`DISTINCT ON … ORDER BY fetched_at DESC`), entrega-os a um *builder* do produtor, que devolve `(inputs, outputs, published_at = fetched_at conferido, snapshot_id, barras a inserir)`, e só então insere manifesto e barras e faz **um** commit (ordem testada com conexão falsa: trava → relógios anteriores → `DISTINCT ON` → builder → `INSERT` do manifesto → `executemany` das barras → commit). A publicação, por sua vez, exige captura posterior à captura atestada pela publicação anterior, `available_at ≥ fetched_at` e `available_at` posterior ao da publicação anterior — as publicações são monótonas nos dois relógios. Duas corridas em disputa no mesmo relógio deixam **exatamente uma** safra; a outra falha com `ValueError` ("does not advance"). Não há mais o caso de dois processos verem ambos "ainda sem linha" para a mesma barra: o pior caso residual é apenas a janela de milissegundos entre o carimbo de `available_at` e o commit da publicação (abaixo).
- **Safra vazia recusada (R8, R13, R14, S2).** Uma corrida cujo conjunto pedido é **vazio** (sem cobertura: nenhuma linha de universo, nenhum símbolo no V3 shadow; ou `--symbols` vazio) é recusada **antes de qualquer tick do relógio, de qualquer requisição ao provedor e de qualquer escrita** (`ValueError` "no symbols to fetch for M" — R14; `persist_run`, `backfill` e `nightly` passam pelo mesmo `_wanted` **antes** de lerem o primeiro instante, S2 — um `Ticks()` vazio nunca levanta `IndexError`); uma corrida cujo conjunto pedido não é vazio mas não obtém barra de **nenhum** símbolo (provedor fora do ar) é recusada **antes de qualquer escrita** (`ValueError` "provider returned no bars for N symbols of M (E errors, R symbols with every row refused, V empty answers)" — erros do provedor, símbolos cujas linhas foram **todas** recusadas por falta de fecho e respostas vazias contados em separado, R13): uma safra vazia nunca esconde a anterior — a safra anterior continua a única que todo corte posterior vê. Basta um símbolo com barras para a corrida valer; os demais ficam registrados como ausentes/erros, como antes.
- **Cada mercado tem a sua vez, e no máximo uma por fase (R9, S1).** `run_all(now=None, *, due_at=None)` (a fase noturna do worker) corre os três mercados **independentemente**: um mercado recusado (sem cobertura, sem barras, relógio que não avança) ou com falha é registado no log (`logger.exception`) e os restantes ainda correm e publicam; no fim, se houve falha, a fase levanta `ValueError` nomeando **só** os mercados falhados (e quantos publicaram) — a fase continua a falhar alto no worker e a tentar de novo dentro da janela (a cada 30 min até as 08h). **Numa nova tentativa, um mercado cuja última publicação é igual ou posterior ao instante em que a fase venceu (`due_at`; por padrão `offhours_due_at(now)` = 01:00 America/Sao_Paulo da data local de `now`, exatamente `start_of_today(now) + start_hour` do `OffhoursPhase` do worker — constante e fuso fixados por teste ao `valuation_worker`) já publicou esta noite e é saltado sem requisição ao provedor nem escrita**, registado como `{"skipped": "already published", "available_at": …}` (e contado na mensagem: "N of 3 markets published, K already published since …"). Assim, enquanto um mercado continua a falhar, as 36 janelas dos outros não são rebuscadas nem republicadas a cada tentativa; na noite seguinte o instante de vencimento avança e todos voltam a correr. `last_run_at` (o mínimo das últimas publicações) segue `None`/antigo enquanto o mercado falhado não publicar — o worker mantém a fase devida, mas só o mercado falhado corre. O `OffhoursPhase` do worker chama a operação sem argumentos, por isso o padrão é o que vale em produção; `due_at` existe para ensaios e para o dia em que o worker passe o instante explicitamente. **Essa primeira conferência é por processo; a garantia forte é do escritor (T5):** o `due` viaja `run_all → nightly → persist_run → Database.persist_price_history_run` e é **reconferido dentro da trava do mercado** (em PostgreSQL dentro da transação com o `pg_advisory_xact_lock`, logo depois de reler os relógios anteriores e antes da leitura de dedupe e de qualquer escrita): se a última publicação relida for igual ou posterior ao `due`, a captura é recusada com `AlreadyPublishedError` (subclasse de `ValueError`: "already published since `<due>`: the latest publication of M is at …, nothing written") — outro processo ou thread publicou o mercado enquanto esta corrida buscava o provedor — e `run_all` regista-a como `{"skipped": "already published", "available_at": …}` com a disponibilidade que o escritor encontrou, nunca como falha. Sem `due` (backfill, `persist_run` direto) a conferência não existe. **Relógios ingénuos (T2):** `run_all` e `offhours_due_at` leem um `now`/`due_at` **sem fuso como hora de parede de São Paulo**, exatamente como `_phase_is_due` (`replace(tzinfo=SAO_PAULO)`) e `start_of_today` (a data ingénua é a data local) do worker — um `01:30` ingénuo é 01:30 em SP (a fase desse dia venceu às 01:00), nunca 01:30 UTC (22:30 da véspera) —, e o mesmo instante alimenta o relógio da corrida; a API de corrida (`persist_run`/`backfill`/`nightly`, `now`/`clock`/`due`) mantém a convenção `_utc` (ingénuo = UTC).
- **Publicação atesta o que foi conferido (R10, R11, S6).** O escritor de publicação recusa, **antes da trava e de qualquer escrita**, uma linha cujo `inputs.fetched_at` não seja uma **string** ISO-8601 (o que a coluna JSON guarda e um escritor futuro relê; um `datetime`, mesmo igual à captura, é recusado — `json.dumps` falharia dentro da transação), não seja parseável ou não seja exatamente a captura conferida (`ValueError` "publication of M attests fetched_at …, not the checked capture … as an ISO-8601 string"); e uma publicação anterior cujo `inputs.fetched_at` esteja ausente ou não parseie (entrou por outra porta) faz **todo** escritor posterior recusar **explicitamente, por id** (`ValueError` "previous publication <id> of M has no parseable fetched_at"), nunca morrer num erro de parse — a cadeia de relógios não pode ser provada. Do lado do leitor, a publicação só é atestada se `inputs.available_at` for **o próprio** `published_at` da linha (a mesma instância, em qualquer fuso): uma disponibilidade "reivindicada" diferente do relógio da linha é `manifest_mismatch`.
- **Duplo em memória fiel à transação, à DDL e à coluna `jsonb` (R12, S3, S4, S5, T3, T4).** `save_analysis_snapshot` em memória guarda o **espelho JSON** de `inputs`/`outputs` (`json.loads(json.dumps(…, default=str))` — objetos novos, nunca os dicionários de quem chamou; um `datetime`/`date`/`Decimal` vira a sua string, um tuplo vira lista, uma chave não-string vira string: o que a coluna `jsonb` devolveria), e os dois escritores da série passam por ele (T4; substitui as cópias profundas); `analysis_snapshot_by_id`, `latest_analysis_snapshot` e `latest_analysis_snapshot_before` devolvem cópias profundas (S4 — como o PostgreSQL, que decodifica JSON novo a cada leitura); a captura valida **todas** as barras **antes** de anexar o manifesto — colunas `NOT NULL` presentes, `fetched_at` parseável, **`session_date` data ISO canónica** (`YYYY-MM-DD` ou `date`; `2026-9-4`, `20260904`, `soon`, mês 13 ou um `datetime` são recusados — a série compara sessões como texto, T4), **`volume`, quando presente, número finito** (int, float ou `Decimal`; nunca bool, string, NaN ou infinito, T4) **e os `CHECK`s da migração 049** (S5: `market` em `('B3', 'NASDAQ', 'NYSE')`, `close` e `adjusted_close` números > 0 — int, float ou `Decimal`; nunca bool, string, NaN ou infinito — **comparados no próprio tipo, sem converter para float: `10**400` é um `NUMERIC` que o PostgreSQL aceita, nunca um `OverflowError`; `Decimal("NaN")` é recusado por `is_finite()` antes de qualquer comparação**, T3 —, `bar_sha256` com 64 hexadecimais minúsculos; `Database._PRICE_BAR_MARKETS` é fixado a `valuation_price_history.MARKETS` e ao texto da DDL por teste) — e, se a anexação das barras ainda falhar, remove **a própria linha do manifesto** da lista (`list.remove`, S3: nunca reconstrói a lista nem apaga outra linha que partilhe o id) — o mesmo "manifesto e barras, ou nenhum" que o rollback do `psycopg` garante em PostgreSQL (o construtor de parâmetros do caminho PostgreSQL faz a mesma validação dentro da transação). Do lado da série, `_number` também engole `OverflowError`: um leitor que recompute o hash de uma linha com um int que não cabe num float devolve `None` (→ `bar_hash_mismatch`), nunca uma exceção.
- **Fonte única**: EODHD `/api/eod` (`eodhd:/api/eod`) para B3 (`.SA`), NASDAQ e NYSE (`.US`), como o inventário declara; uma chamada por símbolo por corrida, na noturna como no backfill.
- **Cobertura**: universo corrente do mercado ∪ todo símbolo com avaliações no V3 shadow (rótulos por vir) — nunca menos.

## Relógio e leitura (anti-lookahead, §2.2)

Todo leitor recebe `available_before` (o corte `C`) e resolve **uma única safra** `S` pelas **publicações deste schema** (`VALUATION-PRICE-HISTORY-v3`): a de maior `available_at < C` (regra única da spec sobre o relógio de disponibilidade — nunca sobre a captura; uma publicação ou manifesto mais novo de outro schema nunca esconde uma safra compatível de um corte histórico). O manifesto é então carregado **pelo id** que a publicação declara e conferido com o que ela atesta (tipo, mercado, schema, `price_snapshot_id`, `bars_sha256`, `fetched_at`) **e com os relógios parseáveis** (`fetched_at`, `from`, `to` do manifesto; `available_at` da publicação — R4); divergência ou relógio ilegível → `manifest_mismatch`, nunca substituída por uma publicação mais antiga, nunca uma exceção no leitor. Para um símbolo, a série servida é "última linha com `fetched_at ≤ fetched_at(S)`" dentro da janela de `S` **menos as sessões que `S` recusou para o símbolo** (R1: o provedor respondeu e a corrida recusou a linha; uma barra de safra anterior para essa sessão não pertence a `S`, e o `series_sha256` gravado nunca a incluiu), com o **hash de conteúdo de cada barra recomputado a partir dos campos lidos** e a série **verificada contra o `series_sha256` que `S` gravou**. **Só leituras `ok` entram no cache** (R5): uma recusa é reavaliada na leitura seguinte, então um manifesto que apareça depois passa a ser servido:

| status | significado |
|---|---|
| `ok` | cada barra reproduz o seu hash e as linhas reproduzem o hash de série de `S` |
| `no_vintage` | nenhuma publicação antes do corte (um corte no meio de uma corrida, ou **entre a captura e a publicação**, não a vê — nem agora nem depois) |
| `manifest_mismatch` | a publicação existe mas o manifesto está ausente ou não confere com o que ela atesta: recusado |
| `symbol_not_in_vintage` | `S` não tem série para o símbolo — **não é completado por uma safra anterior** |
| `vintage_unreproducible` | a própria corrida detectou uma sessão que o provedor deixou de devolver (**linha nenhuma** — uma linha recusada por falta de fecho não conta: é `adjustment_unknown` no rótulo, R1): declarado no manifesto, recusado |
| `bar_hash_mismatch` | uma linha armazenada não reproduz o próprio `bar_sha256` (conteúdo alterado sob o hash antigo): o símbolo inteiro é recusado |
| `vintage_mismatch` | as linhas não reproduzem o hash de série gravado: recusado |

**Três relógios acompanham toda leitura** (cabeçalho de `series` e todo rótulo `labelled`): `available_at` (publicação de `S` — o relógio que o pacote compara com o corte; §2.2 "available_at(P_real)"), `fetched_at` (captura de `S`) e `first_captured_at` (o `fetched_at` da própria linha: a **primeira captura daquele conteúdo**, que sob dedupe pode ser de uma corrida anterior; no cabeçalho da série é a mais antiga entre as barras servidas). `series` expõe ainda `window` = `[from, to]` da safra e `rejected_sessions` = sessões que a corrida recusou para o símbolo com os campos ausentes (uma linha incompleta duplicada de uma linha utilizável da mesma sessão é ruído, não recusa). **Todo leitor devolve cópias profundas**: mutar o que se recebeu nunca altera o cache nem a leitura seguinte — e o resultado de `persist_run` também é cópia profunda, nunca os dicionários do manifesto armazenado (R3).

`label_bar(mercado, símbolo, sessão, h, available_before)` localiza a sessão `h` pregões à frente no calendário da bolsa (`exchange_calendars` 4.13.2: XNYS para NASDAQ/NYSE, BVMF para B3 — a spec grafa `XBSP`; errata documental para uma rev futura) e devolve `not_a_session`/`beyond_calendar` (calendário), `not_yet_mature` **enquanto o fecho da sessão alvo (hora da bolsa, não a data civil) não for anterior ao corte**, o status da safra quando ela recusa o símbolo, `outside_window` (a sessão alvo está fora da janela que a safra pediu), **`label_unavailable:adjustment_unknown`** com `missing` (a corrida recusou a linha do provedor para essa sessão por faltar `close`/`adjusted_close` em `S` — §2.2; nunca `missing_bar` nesse caso), `missing_bar` (a bolsa teve sessão, a safra tem o símbolo, a janela cobre a sessão e o provedor não devolveu linha alguma) ou `labelled` com `close`, `adjusted_close`, `bar_sha256`, `price_snapshot_id`, `publication_id` e os três relógios — nunca um preço de outra fonte.

O backfill de 36 meses é capturado agora e fica disponível na sua publicação, portanto só pacotes cortados **depois** dela o consomem — história para treino, nunca antevisão. Limitação declarada e inerente a qualquer armazenamento: entre o carimbo de `available_at` e o commit da linha de publicação há uma janela de milissegundos; ela é mantida conservadora (o carimbo é o último passo antes do insert) e um corte dentro dela **não vê** a safra (`<` estrito) — o erro possível é sempre "ainda não disponível", nunca "disponível antes de existir".

## Operação

- **OFF por padrão.** A fase noturna (`OffhoursPhase("price_history")`) só entra no worker de valuation com `C3PO_VALUATION_PRICE_HISTORY_ENABLED=true`; o cartão de saúde só a espera quando ligada (o prazo de cada fase ligada é derivado das definições ativas, e um sucesso da fase nunca derruba o cartão).
- **Backfill (uma vez, por ordem da mesa):** `python -m app.valuation_price_history --market NASDAQ --backfill` (idem NYSE e B3); `--months` sobrepõe a configuração; `--symbols A,B` restringe a cobertura em ensaios. A saída JSON omite os mapas por símbolo (ficam no manifesto). Um provedor fora do ar (nenhum símbolo com barras) ou uma cobertura vazia termina com `ValueError` e nada escrito (R8, R14).
- **Relógio (R6):** `persist_run` lê o relógio **exatamente três vezes** por corrida — `started_at` (antes da primeira requisição), `fetched_at` (depois da última resposta) e `available_at` (dentro do escritor da publicação); `backfill`/`nightly` derivam o fim da janela do **primeiro** desses instantes, nunca de um quarto tick, e um conjunto vazio é recusado antes desse primeiro tick (S2). Um relógio roteirizado (`clock=`) precisa portanto de três instantes.
- **Runbook — publicação anterior envenenada (R10, T1).** Sintoma: a fase noturna do mercado `M` falha todas as noites com `ValueError` "previous publication `<id>` of M has no parseable fetched_at (…)" e os leitores devolvem `manifest_mismatch` para os cortes posteriores a essa linha — a safra anterior continua servida para os cortes que a precedem. A linha está identificada **pelo id** na própria mensagem: `SELECT id, published_at, inputs FROM analysis_snapshots WHERE id = '<id>'` mostra o `inputs.fetched_at` ausente ou ilegível (a linha entrou por outra porta: os escritores deste módulo nunca a gravam, R10/S6). **Enquanto essa linha for a última publicação de `M`, os dois escritores deste módulo — captura e publicação — recusam por id, qualquer que seja o schema:** os leitores da publicação anterior (`_price_history_previous_memory`/`_pg`) não filtram por `schema_version`, por desenho (a cadeia de relógios atravessa schemas), portanto subir `SCHEMA_VERSION` **não** destrava nada, e o próprio módulo **não consegue** gravar a publicação corretiva (o escritor de publicação relê a envenenada e recusa). Não há correção automática nem "pular" a linha. **As únicas saídas são manuais e só sob ordem da mesa** (registro no canal com o id e a causa): **(a)** o operador do banco corrige o `inputs.fetched_at` **dessa linha** para a captura que ela atesta (o `fetched_at`/`published_at` do manifesto que o seu `price_snapshot_id` aponta), com `UPDATE analysis_snapshots SET inputs = jsonb_set(inputs, '{fetched_at}', to_jsonb('<fetched_at do manifesto, ISO-8601>'::text)) WHERE id = '<id>'` — verificado: `analysis_snapshots` (`db/001_initial.sql`) **não tem gatilho algum** (nenhuma migração cria `TRIGGER`/`RULE`/`POLICY` sobre ela; fixado por teste), ao contrário de `valuation_price_bars`, que é append-only por gatilho — depois disso a linha passa a atestar o seu manifesto (servida para os seus cortes) e a captura seguinte avança; ou **(b)** o operador insere manualmente, **depois** dela, uma publicação bem formada para o **mesmo** manifesto: `analysis_type = 'valuation_price_history_publication'`, `entity_key = M`, `published_at` = agora, `inputs = {schema_version, price_snapshot_id, fetched_at (= o do manifesto, string ISO-8601), started_at, available_at (= o próprio published_at)}`, `outputs = {schema_version, bars_sha256 (= o do manifesto), price_snapshot_id}` — a linha envenenada continua recusada para os seus próprios cortes (`manifest_mismatch`), a nova passa a ser a última e a captura seguinte avança. Até lá o worker continua a tentar dentro da janela e a falhar alto: é o comportamento esperado, não um bug a silenciar.
- **Escopo do pyright.** `app/database.py` está na lista `exclude` de `pyrightconfig.json` (a CI, `pyright --pythonpath "$(command -v python)"`, não o verifica); as funções da série de preços que vivem nele são conferidas **manualmente** com `pyright --pythonpath "$(command -v python)" app/database.py` a cada revisão — o resultado (número de erros) é reportado no PR. Em 08/09/2026 (rev 3, reconferido após T1–T5): `0 errors, 0 warnings, 0 informations` no ficheiro inteiro (pyright do `venv312`, interpretador do `venv-py9`).
- **Não faz:** não calcula erros, pesos ou rótulos além de localizar a barra; não altera V1/V2/V3; não toca no R2D2.

## Testes

`tests/test_valuation_price_history.py`: hash de conteúdo (mesma barra em dois relógios = mesmo hash), dois fechos obrigatórios, `bar_defects` (sessão + campos ausentes), hash recomputável dos campos armazenados, canonização a 6 casas; cobertura = universo ∪ V3 shadow; corrida = captura (manifesto + barras numa única escrita transacional) **antes** da publicação, hashes por símbolo, dedupe por conteúdo, ausentes/erros/rejeitadas (com sessão e campo; fora da janela não conta) registrados; **captura ≠ publicação** (relógio 23:00/23:01/23:03: cortes dentro da corrida, na captura, entre captura e publicação e na publicação = `no_vintage`, e continuam assim depois da escrita; manifesto órfão invisível; publicação cujo manifesto não confere = `manifest_mismatch`); um corte vê exatamente uma safra e um símbolo ausente de S2 é recusado, não completado por S1; linha estranha auto-consistente = `vintage_mismatch`; **três relógios sob dedupe** (rótulo com `available_at`/`fetched_at` de S2 e `first_captured_at` de S1); **linha alterada sob o hash antigo = `bar_hash_mismatch`** e mutação do que se recebeu não altera a leitura seguinte; **sessão recusada = `label_unavailable:adjustment_unknown`** com os campos, nunca `missing_bar` (e `missing_bar` quando não há linha alguma); sessão descartada pelo provedor = `vintage_unreproducible`; relógios como instantes reais (fuso de SP vs UTC) e duplicata no mesmo lote = uma linha; **captura que não avança além do manifesto ou da publicação anterior é recusada** (e o escritor de publicação recusa captura regressiva, relógio para trás e disponibilidade repetida); **duas threads em disputa no mesmo relógio = um manifesto e uma publicação, a outra recusada**; caminho PostgreSQL com conexão falsa: `pg_advisory_xact_lock` é a primeira instrução, os dois relógios são relidos, recusa sem leitura de dedupe/insert/commit, **ordem trava → relógios → `DISTINCT ON` → builder (com os hashes lidos) → `INSERT` do manifesto → `executemany` → um commit** (R2), builder com relógio diferente do conferido recusado, `supersedes_id` correto, `available_at` lido uma única vez após as checagens; em memória o builder roda com a trava do mercado tomada (outra thread não a obtém) e o manifesto armazenado não é o dicionário do builder; rótulos no calendário (feriado de 07/09), maturidade pelo fecho, `outside_window`; janela do backfill e da noturna, configuração viva, `BRK.B`, `last_run_at` pelas publicações, fase dormente e DDL (FK, gatilhos, TRUNCATE, publicação e trava declaradas). **Residuais R1–R8:** sessão recusada em S2 que S1 tinha armazenado = `adjustment_unknown` com o campo, 09-09 `labelled`, série `ok`, `symbols_unreproducible` vazio (e sessão sem linha alguma continua `vintage_unreproducible`; duplicata incompleta de linha utilizável não é recusa); resultado de `persist_run` mutado não altera a leitura seguinte nem o manifesto; publicação/manifesto sem `fetched_at`, sem `from`/`to` legíveis ou sem `available_at` = `manifest_mismatch` sem exceção (e o mesmo manifesto com relógios legíveis é servido); `manifest_mismatch` não entra no cache e o manifesto que aparece depois é servido; `backfill`/`nightly` com `Ticks` de exatamente três instantes (um quarto nunca é pedido; script de dois falha dentro da corrida sem publicar); corrida sem barra de símbolo algum recusada com nada escrito e a safra anterior continua servida. **Residuais R9–R14:** `run_all` com B3 (primeiro em `MARKETS`) recusado por resposta vazia → NASDAQ/NYSE publicam, B3 sem manifesto nem publicação, `ValueError` no fim nomeando só B3 (e "2 of 3 markets published"), `logger.exception` com o traceback, `last_run_at` continua `None`; dois mercados recusados (sem cobertura + provedor fora) nomeados juntos; três mercados bons = três resultados; publicação com `inputs.fetched_at` ausente/vazio/ilegível/diferente da captura conferida recusada antes de escrever (mesmo instante noutro fuso aceite); publicação anterior envenenada (ilegível ou ausente) faz captura e publicação seguintes recusarem **por id**, nada escrito, e o leitor devolve `manifest_mismatch` sem exceção — em memória e com conexão PostgreSQL falsa (trava → dois relógios → recusa; sem `DISTINCT ON`, builder, insert ou commit; inputs inconsistentes recusados **antes** da trava, log vazio); `available_at` atestado antes/depois do `published_at` da linha = `manifest_mismatch` (cabeçalho com o relógio da linha), mesmo instante em fuso de SP = `ok`; cópias profundas na publicação e em `analysis_snapshot_by_id`; barra sem coluna `NOT NULL` ou com `fetched_at` ilegível → captura recusada sem manifesto nem barra, a mesma captura com barras boas passa **no mesmo relógio**, falha na anexação remove o manifesto, `_price_bar_params` recusa igual; mensagem da R8 com "1 errors, 1 symbols with every row refused, 3 empty answers" (linha recusada fora da janela = resposta vazia); conjunto pedido vazio (`[]`, `["", "  "]`, `backfill --symbols` vazio, `nightly` sem cobertura) recusado sem tick (`Ticks()` vazio não levanta `IndexError` em `persist_run`, `backfill` **e** `nightly`; um `Ticks(t)` fica com o instante por consumir — S2), sem chamada ao provedor, nada escrito. **Terceira reverificação (S1–S6):** `run_all` chamado três vezes na mesma noite (01:05, 01:35, 02:05 de SP) com B3 sem cobertura → NASDAQ/NYSE com **um** manifesto e **uma** publicação cada, provedor consultado **uma** vez por mercado, B3 recusado nas três, mensagem com "2 already published since …" nas tentativas seguintes, `logger.info` "… already published at … skipped", `last_run_at` `None`; B3 ganha cobertura na quarta tentativa → só B3 corre, os outros vêm como `{"skipped": "already published", "available_at": …}`, `last_run_at` = publicação de NASDAQ e `_phase_is_due` do worker diz "não devida"; noite seguinte → os três correm; `due_at` explícito (UTC e fuso de SP) salta tudo o que foi publicado a partir dele sem chamada ao provedor, `due_at` um segundo depois das publicações não salta nada (a re-execução é tentada e recusada pelo relógio constante); `run_all()` sem argumentos usa o relógio real e, sem cobertura, recusa os três sem tick nem chamada; `OFFHOURS_DUE_HOUR`/`SAO_PAULO` fixados a `valuation_worker.OFFHOURS_START_HOUR`/`SAO_PAULO`, `offhours_due_at(now)` = `start_of_today(now_SP) + start_hour` para instantes UTC, SP, ingénuos e virada de dia (01:30 UTC de 08/09 → vencimento de 07/09), e o salto é o complemento de `_phase_is_due`; rollback em memória com uma linha de outro tipo partilhando o id → só o manifesto sai, as outras linhas são os mesmos objetos (S3); `latest_analysis_snapshot`/`latest_analysis_snapshot_before` (com e sem `schema_version`) devolvem cópias profundas e mutar a publicação lida não envenena o corte seguinte (S4); `CHECK`s espelhados — mercado fora do enum ou em minúsculas, `close` 0/negativo/string/bool/NaN/inf, `bar_sha256` maiúsculo/63 chars/com newline final/inteiro → recusados por `insert_price_bars`, `_price_bar_params` e `persist_price_history_run` sem manifesto nem barra; `Decimal`, `int`, volume qualquer e os três mercados aceites; `_PRICE_BAR_MARKETS` = `MARKETS` e texto dos `CHECK`s na DDL (S5); `inputs.fetched_at` como `datetime` (aware ou ingénuo) igual à captura conferida recusado **antes da trava** (em memória a trava é substituída por `pytest.fail`; com conexão falsa o log fica vazio) e antes do carimbo (S6). **Quarta reverificação (T1–T5):** publicação envenenada como última linha → captura e publicação recusadas por id, ainda com `SCHEMA_VERSION` trocado por `monkeypatch` (e `run_all` idem), leitores em `manifest_mismatch` para os seus cortes e a safra anterior servida para os anteriores; saída (a) corrigir `inputs.fetched_at` da linha → a captura seguinte avança e a linha corrigida passa a ser servida; saída (b) publicação bem formada inserida manualmente para o mesmo manifesto depois de uma segunda envenenada (sem `fetched_at`) → servida, a captura seguinte avança, a envenenada continua `manifest_mismatch`; as migrações não declaram `TRIGGER`/`RULE`/`POLICY` sobre `analysis_snapshots` (e declaram gatilho sobre `valuation_price_bars`) (T1); instantes ingénuos `00:00`, `01:30` e `02:59:59` → `offhours_due_at(now) == worker.start_of_today(now) + 1h` **sem conversão prévia** (01:30 ingénuo = 04:00 UTC do dia 8, não 04:00 UTC do dia 7), `_worker_instant` = `replace(tzinfo=SAO_PAULO)`, e `run_all(now=01:30 ingénuo)` com `due_at` ingénuo 01:00 salta tudo o que publicou às 01:05 SP, com 01:10 corre de novo e publica às 04:30 UTC (o relógio da corrida também é a leitura de SP) (T2); `_positive_price(10**400)` aceite, `-(10**400)`/`Decimal("NaN")`/`Decimal("Infinity")`/`True` recusados sem exceção, barra com `close = 10**400`, `adjusted_close = Decimal("1e400")` e `volume = -(10**400)` inserida, `_number`/`bar_defects`/`bar_content_sha256` devolvem `None`/campo em falta em vez de `OverflowError` (T3); `session_date` `soon`/`2026-9-4`/`20260904`/`2026-13-01`/int/`datetime` e `volume` bool/string/NaN/±inf/`Decimal("NaN")`/`Decimal("Infinity")` recusados por `insert_price_bars`, `_price_bar_params` e `persist_price_history_run` sem manifesto nem barra; `volume` 0/`Decimal`/`None`/int gigante e `session_date` como `date` aceites; `save_analysis_snapshot` e os dois escritores guardam o espelho JSON (`datetime`/`date`/`Decimal` → string, tuplo → lista, chave `3` → `"3"`, `published_at` continua `datetime`), os dicionários de quem chamou ficam intactos e mutá-los depois não altera a leitura (T4); `run_all` com outro serviço a publicar NASDAQ **de dentro da resposta do provedor** (depois da primeira conferência, antes da trava) → NASDAQ `{"skipped": "already published", "available_at": <publicação do outro>}`, um só manifesto e uma só publicação (do outro), B3/NYSE publicam, `logger.info` "… while this run was fetching: skipped, nothing written", `last_run_at` = a publicação do outro; `persist_run(due=…)` direto → `AlreadyPublishedError` (subclasse de `ValueError`, com `due`/`available_at`/`entity_key`), `due` posterior à publicação → corre, sem `due` (backfill) → nunca recusado por isso; com conexão PostgreSQL falsa → trava → dois relógios → recusa "already published since …", sem `DISTINCT ON`, builder ou commit (T5).
